name: Test GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "GitHub Actions is working for Jahid's AWS Portfolio"